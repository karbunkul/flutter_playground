import 'package:flutter/material.dart';

class ListStickyHeaderPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('List Sticky Header'),
      ),
    );
  }
}
